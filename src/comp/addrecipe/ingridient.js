import React from 'react';

const RecipeIngridients = (ingridient) => {

    return (
        <div>
            <div>{ingridient}</div>
            <button>X</button>
        </div>
    )
}

export default RecipeIngridients;